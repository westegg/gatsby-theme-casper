{
  "private": true,
  "workspaces": [
    "theme",
    "demo"
  ],
  "repository": "westegg/gatsby-theme-casper",
  "scripts": {
    "build": "bash -c \"cd theme && yarn build\" && bash -c \"cd demo && yarn build\""
  }
}
